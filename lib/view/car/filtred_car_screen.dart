import 'package:flutter/material.dart';

class FiltredCarScreen extends StatelessWidget {
  const FiltredCarScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
